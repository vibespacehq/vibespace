# Changelog

All notable changes to vibespace will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [0.2.0] - 2026-02-08

### Bug Fixes

- **remote:** Make QuickUp() accept address param for server mode
- **platform:** Configure k3s TLS SAN for WireGuard IP
- **remote:** Add rate limiting and request size limits
- **remote:** Add TLS to management API with cert pinning
- **remote:** Harden security headers, TLS 1.3, permissions
- Harden file permissions and SSH security
- **remote:** Ensure cert fingerprint is included in invite token
- **remote:** Remove stale manual key exchange instructions
- Code quality fixes from E2E testing and audit
- **uninstall:** Stop serve process and clean up WireGuard fully
- **remote:** Reinitialize WireGuard keys when key file is missing
- **remote:** Verify signing key matches before reusing
- **uninstall:** Skip stat check on /etc/wireguard (root-only dir)
- **remote:** Detect and tear down stale WireGuard interface on serve start
- **serve:** Reload server state after daemon spawn to prevent key race
- **uninstall:** Kill serve daemon by PID instead of Stop()

### Documentation

- Update CLI_STATUS.md with completed P6 versioning tasks
- Add DEPENDENCIES.md
- Add remote mode E2E test protocol
- Reorganize and clean up documentation

### Features

- Add git-cliff for changelog generation
- Add resource overcommit for higher agent density
- **remote:** Add self-signed TLS cert generation and fingerprint pinning
- **remote:** Add one-shot registration API (server side)
- **remote:** One-shot client registration and connect
- **remote:** Add auto-reconnect connection watcher
- **remote:** Add firewall detection and client diagnostics
- **remote:** Enhanced status command with diagnostics
- **remote:** Add client management and graceful disconnect
- Add embedded DNS server for *.vibespace.internal
- **remote:** Add watch command for auto-reconnect
- **dns:** Wire resolver and records into daemon
- **remote:** Make all CLI commands remote-mode aware

### Refactor

- **remote:** Remove deprecated activate command

## [0.1.0] - 2026-02-03

### Bug Fixes

- Bundle shared libraries with QEMU binaries
- Bundle QEMU firmware files (BIOS, VGA ROMs)
- Wrapper scripts - use BASH_SOURCE, no -L for qemu-img
- Handle Lima 2.0 JSON output format
- SSH exec and --help handling for Linux compatibility
- Proper daemonization for vibespace serve
- Use apt and system paths for WireGuard on Linux
- Use apt-get and remove -E flag on Linux
- Bundle bash 4+ for macOS, require --ip for add-client
- Request plain text from IP detection services
- Use wireguard-go directly on macOS, no bash needed
- Read address from RemoteState instead of /etc/wireguard
- Use utun interface name on macOS
- Harden remote tokens and improve WireGuard flow
- Fix IPv6 endpoint port formatting
- Prefer IPv4 for public endpoint detection
- Start kube proxy in foreground mode

### CI/CD

- Trigger QEMU build on push to vibespace-wip-linux

### Documentation

- Add --sync idea for remote mode file mounting
- Add remote mode progress note

### Features

- Add Linux support using Lima VM with k3s
- Add QEMU binary download for Linux support
- Add --mount flag and info command
- Add remote mode with WireGuard tunnel support
- Auto-detect public IP for --generate-token
- Proxy kube API over WireGuard
- Bundle Docker CLI with vibespace
- Add build date to version info and fix ldflags path

### Miscellaneous

- Remove unused code

### Refactor

- Remove SSH from remote mode, use copy-paste key exchange
- Simplify remote mode flow with copy-paste IP exchange

---
Generated by [git-cliff](https://github.com/orhun/git-cliff)
