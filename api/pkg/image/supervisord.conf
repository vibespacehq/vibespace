; vibespace supervisor configuration
; Manages: GoTTY web terminal

[supervisord]
nodaemon=true
logfile=/var/log/supervisor/supervisord.log
pidfile=/tmp/supervisord.pid
childlogdir=/var/log/supervisor

[unix_http_server]
file=/tmp/supervisor.sock

[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface

[supervisorctl]
serverurl=unix:///tmp/supervisor.sock

; GoTTY - Web terminal server (Go-based, compatible with gotty-client)
; Runs as non-root user, serves on port 7681
[program:gotty]
command=/usr/bin/gotty -w --port 7681 /bin/bash
user=user
directory=/vibespace
autostart=true
autorestart=true
stdout_logfile=/var/log/supervisor/gotty.log
stderr_logfile=/var/log/supervisor/gotty-error.log
environment=HOME="/home/user",USER="user",PATH="/home/user/.local/bin:/home/user/.npm-global/bin:/usr/local/bin:/usr/bin:/bin"
