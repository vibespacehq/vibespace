# Codecov configuration for vibespacehq/vibespace
coverage:
  status:
    project:
      default:
        target: 0%
        threshold: 0%
    patch: off

comment:
  layout: "reach,diff"
  behavior: default
