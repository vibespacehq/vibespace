coverage:
  status:
    project:
      default:
        target: auto
        threshold: 2%
    patch:
      default:
        target: auto

flags:
  unit:
    paths:
      - "**/*.go"
    carryforward: true
  integration:
    paths:
      - "**/*.go"
    carryforward: true
  e2e-baremetal:
    paths:
      - "**/*.go"
    carryforward: false
  e2e-colima:
    paths:
      - "**/*.go"
    carryforward: false
  e2e-lima:
    paths:
      - "**/*.go"
    carryforward: false

comment:
  layout: "reach,diff,flags"
  behavior: default
